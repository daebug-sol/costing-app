/** @deprecated Prefer @/lib/utils/format — kept for gradual migration */
export {
  formatIDR,
  formatIDR as formatIdr,
  formatNumber,
  formatNumber as formatNumberId,
  formatPercent,
} from "@/lib/utils/format";
