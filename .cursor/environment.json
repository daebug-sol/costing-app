{
  "name": "costing-app-next-prisma-playwright",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "npm ci && npx playwright install-deps chromium && npx playwright install chromium && npx prisma generate"
}
